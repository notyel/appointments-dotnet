@model IEnumerable<AppointmentSystem.Application.DTOs.BranchDto>
@{
    ViewData["Title"] = "Seleccione una Sucursal";
}

@if (User.IsInRole("Admin") || User.IsInRole("BranchAdmin") || User.IsInRole("Receptionist"))
{
    <div class="alert" style="background-color: #e3f2fd; color: #1565c0; border: 1px solid #90caf9; margin-bottom: 2rem;">
        <strong>👋 Hola, @User.Identity.Name</strong>
        <p style="margin: 0.5rem 0 0 0;">
            <a asp-controller="Admin" asp-action="Index" class="btn btn-primary" style="margin-right: 0.5rem;">Ir al Panel de Administración</a>
            <span style="color: #666;">o continúa navegando como cliente</span>
        </p>
    </div>
}

<h2>Bienvenido a nuestro centro de estética</h2>
<p>Por favor, seleccione la sucursal donde desea agendar su cita:</p>

<div class="grid">
    @foreach (var branch in Model)
    {
        <div class="card">
            <h3>@branch.Name</h3>
            <p>@branch.Address</p>
            <p>Tel: @branch.Phone</p>
            <a asp-controller="Booking" asp-action="Services" asp-route-branchId="@branch.Id" class="btn btn-primary">Ver Servicios</a>
        </div>
    }
</div>

